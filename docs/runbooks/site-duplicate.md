# Duplicate Live Site

Dry-run:

```bash
sudo platform-v2 site duplicate   --from=nvh   --name=nvh-test-2   --domain=nvh-test-2.example.com   --copy-db   --dry-run
```

The output must show:

```text
Strategy : duplicate-live
```
