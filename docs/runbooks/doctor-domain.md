# Domain Doctor

Unused domain:

```bash
sudo platform-v2 doctor domain inafo.tungocvan.com
```

Existing domain:

```bash
sudo platform-v2 doctor domain ntd.tungocvan.com
```

The existing-domain report includes site/database/path/ports and backup state.
