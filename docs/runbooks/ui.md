# Deploy Wizard Runbook

```bash
sudo platform-v2
```

Then:

```text
Deploy
→ choose site
→ Frontend
→ Production Build
```

This is the menu equivalent of rebuilding frontend assets (`npm run build`,
`pnpm run build`, or `yarn run build`) without needing to remember the package
manager command.
