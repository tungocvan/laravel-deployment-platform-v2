# Interactive UI

The UI is intentionally thin:

```text
Menu
→ collect parameters
→ preview/dry-run
→ confirm
→ existing platform-v2 command
```

No business logic should be copied into this module.
