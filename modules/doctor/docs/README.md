# Doctor Module

Doctor is read-only preflight.

Initial command:

```bash
platform-v2 doctor domain <domain>
```

Future Site Duplicate and Backup Restore can consume the same helper instead of reimplementing domain checks.
